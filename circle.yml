machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: v6.1.0
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
